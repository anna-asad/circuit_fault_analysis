# TODO - UI cleanup & results page

- [x] Implement non-blocking in-UI value editing for components (remove window.prompt/window.alert)


- [ ] Replace alert-based validation/errors in SimulateButton with inline UI messaging
- [x] Add a full-screen “Results page” view that replaces the canvas after simulation

- [ ] In results view: render DC source as only “−” and “+” signs (no numeric value button)


- [ ] Improve edge (wire) rendering style for cleaner look

- [ ] Clean up code in modified React components (remove unnecessary wrappers)
- [ ] Verify with `npm run dev` and lint/tests if available


